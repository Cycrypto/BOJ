#define _CRT_SECURE_NO_WARNINGS
#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;
int main(void) {
	int N, M = 0;
	scanf("%d %d", &N, &M);
	vector <int> vec;

	for (int i = 0; i < N; i++) {
		int input = 0;
		scanf("%d", &input);
		vec.push_back(input);
	}	// 벡터에 값 입력

	for (int i = 0; i < N; i++) {
		for(int j = 0; j < )
	}
}
